# ==========================================
# 图片管理系统 - 环境变量配置示例
# ==========================================
# 复制此文件为 .env.local 并修改配置

# ==========================================
# 数据库配置（重要 - 数据持久化必需）
# ==========================================
# PostgreSQL 连接字符串
# - 沙箱环境：自动注入 PGDATABASE_URL
# - 本地开发：需要手动配置，或使用自己的数据库
# 
# 示例：
# PGDATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
#
# 注意：沙箱数据库是内网地址，本地无法直接访问
# 本地开发建议使用 Docker 启动 PostgreSQL：
#   docker run -d --name postgres -e POSTGRES_PASSWORD=password -p 5432:5432 postgres:16

# ==========================================
# 后端 API 配置
# ==========================================

# Java 后端 API 地址
# - 如果配置且后端可用 → 使用 Java 后端 API
# - 如果不配置或后端不可用 → 使用 Next.js 降级模式
# 
# 示例值：
# - 本地开发：http://localhost:8080/api
# - ngrok 代理：https://your-ngrok-url.ngrok-free.app/api
# - 生产环境：http://your-java-server:8080/api
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8080/api

# ==========================================
# ngrok 代理配置（外网访问）
# ==========================================
# 使用 ngrok 将本地服务暴露到公网
# 
# 1. 安装 ngrok: https://ngrok.com/download
# 2. 注册账号并获取 token: https://dashboard.ngrok.com/get-started/your-authtoken
# 3. 配置 token: ngrok config add-authtoken <your-token>
# 4. 启动代理: ngrok http 5000
# 5. 复制 ngrok 提供的 https 地址（如 https://xxxx.ngrok-free.app）
# 6. 更新 NEXT_PUBLIC_BACKEND_API_URL 为 ngrok 地址
#
# 注意：
# - 免费版 ngrok 每次启动域名会变化，需要更新配置
# - 付费版可以绑定固定域名
# - 后端 CORS 已配置允许所有域名，无需额外修改

# ==========================================
# 对象存储配置（沙箱环境已内置）
# ==========================================
# 以下变量由沙箱环境自动注入，无需手动配置
# COZE_BUCKET_ENDPOINT_URL - 对象存储端点 URL
# COZE_BUCKET_NAME - 存储桶名称

# ==========================================
# 认证配置（可选）
# ==========================================
# NEXTAUTH_SECRET=your-secret-key
# NEXTAUTH_URL=http://localhost:5000
