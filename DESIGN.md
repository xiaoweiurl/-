# DESIGN.md

## 项目与用户画像
- **定位**：AI数据中台（宝娜斯产品智能中台），面向服装/纺织行业的企业级数据智能平台
- **用户**：设计师、工厂供应链人员、市场营销人员、企业管理者
- **核心理念**：数据驱动 + AI赋能 + B端专业感与iOS精致感的平衡

## 品牌与视觉方向
- **主风格**：iOS / macOS 原生设计风格（Apple Design），对标苹果官方「文件」「系统设置」App 质感
- **可扩展性**：设计系统同时覆盖设计师板块与 ERP 业务板块，新增页面视觉语言必须统一
- **禁止**：Material Design / 安卓 / Windows 风格元素；高饱和荧光色；渐变背景；深色模式混搭；彩色描边；发光效果；拟物化纹理；花哨装饰

## Design Tokens

### 色彩（精确色值，禁止自行调整饱和度）
- **主色**：`#007AFF`（iOS 系统蓝，仅用于选中态、主按钮、关键强调）
- **功能色**：成功 `#34C759` / 警告 `#FF9500` / 错误 `#FF3B30` / 辅助 `#AF52DE`
- **背景**：全局/分组背景 `#F2F2F7`，卡片背景 `#FFFFFF`
- **文字**：一级标题 `#1C1C1E` / 二级正文 `#3A3A3C` / 辅助说明 `#8E8E93`
- **分割线**：`#E5E5EA`，极细 1px，仅用于列表与边界分隔
- **状态标签**：对应功能色的淡色背景（如 `rgba(52,199,89,0.12)` 底 + `#34C759` 字）

### 材质与质感
- **顶部导航 / 侧边栏**：毛玻璃 `backdrop-filter: blur(20px)` + 背景 `rgba(255,255,255,0.72)`
- **内容卡片**：纯白实底，无描边，靠阴影区分层级
- **禁止**：发光效果、渐变边框、浮雕效果

### 圆角（统一层级，禁止乱混）
- 大卡片 / 大模块：`20px`（rounded-2xl 层级）
- 小按钮 / 输入框 / 标签：`12px`（rounded-xl 层级）
- 头像 / 小圆点：圆形
- **禁止**：直角元素、同组件圆角不一、大于 24px 的夸张圆角

### 阴影（轻量是灵魂）
- 默认态：`0 2px 12px rgba(0, 0, 0, 0.04)`
- 悬停态：`0 6px 24px rgba(0, 0, 0, 0.08)`
- **禁止**：黑色浓阴影、多层阴影叠加、外发光

### 字体与排版
- 字体栈：`-apple-system, BlinkMacSystemFont, "SF Pro Display", "Segoe UI", Roboto, sans-serif`
- 字号层级：大标题 30px 粗体 / 模块标题 18px 半粗 / 正文 14px / 辅助文字 12px
- 行高宽松，保持呼吸感，禁止紧凑排版
- 所有数字使用等宽样式（`font-variant-numeric: tabular-nums`），保证数据对齐

### 间距
- 页面左右边距：24px；模块之间间距：24px；卡片内边距：20px
- 列表行高：48-56px
- **禁止**：排版拥挤、边距不一致、信息堆砌

### 动效
- 卡片悬停：向上轻微偏移 3px + 阴影加深，过渡 0.3s 缓动
- 按钮点击：轻微缩放反馈（active:scale-95/98）
- 导航切换：平滑高亮过渡
- **禁止**：夸张弹跳、旋转特效

## 布局与响应式
- 桌面端：左侧固定侧边导航 + 顶部全局导航栏（固定悬浮毛玻璃）+ 右侧主内容区
- 移动端：隐藏侧边栏改底部 Tab 标签栏，顶部保留大标题导航
- 侧边栏分组：业务菜单 + 系统菜单；选中态蓝底白字圆角高亮；未选中灰黑文字、悬停浅灰背景
- 顶栏：左侧 Logo+名称，中间搜索框（内嵌图标、浅灰背景、圆角），右侧通知铃铛（红点）+ 用户头像名称
- 内容卡片层级：白底 + 轻阴影，无描边
